Gym Tracker — Handover Document (current as of app v6.9 / sw v56 / schema v20, 2026-07-20)

============================================================
1. WHAT THIS IS
============================================================
A single-file Progressive Web App that is two things fused together:

• A gym workout tracker — logs weight × reps × sets per exercise per day. This is the
  real, load-bearing purpose (the user, Mateusz, is a gym-goer who tracks his lifts).
• A Pokémon RPG built on top of it — every set you log powers a full Pokémon game
  (battle catching, leveling, boss battles, special buildings, an economy with two
  currencies, a Pokédex with official artwork, medals, quests, fishing, a decathlon,
  a battle tower). The workout IS the game engine. It is TWO campaigns: a FireRed
  KANTO campaign and an Emerald HOENN campaign, chosen with a region switcher
  (S.S. Tidal in OPTION, unlocks after the Kanto Champion). Each region is its own
  self-contained save; workout data is shared. As of v6.1 the Hoenn campaign carries
  the FULL Team Magma/Aqua story arc (8 chapters) + twelve special places.

Design conceit: it had to be ONE PWA, not a separate game app, because iOS gives each
installed home-screen app isolated storage — a separate app couldn't read the workout logs.

Live URL:  https://mateuszkrw-coder.github.io/gym-tracker-pokemon-game/
Repo:      https://github.com/mateuszkrw-coder/gym-tracker-pokemon-game  (GitHub Pages from main branch root;
           renamed from gym-tracker, whose old Pages URL now 404s)
Current:   app v6.9 “Level Rush”, service-worker cache v56, data schema v20.

============================================================
2. THE TWO-LAPTOP WORKFLOW (read this FIRST on any machine)
============================================================
Mateusz works from (at least) TWO Windows laptops. Known so far:
  • Machine A: D:\gym app  — Windows user "dctek"  (memory dir:
    C:\Users\dctek\.claude\projects\D--gym-app\memory\)
  • Machine B: E:\gym app  — Windows user "Satsuki" (memory dir:
    C:\Users\Satsuki\.claude\projects\E--gym-app\memory\)
  • Machine C (since 2026-07-12): C:\Users\Satsuki\Desktop\gym-tracker-main\gym-tracker-main
    — Windows user "Satsuki" (a downloaded repo ZIP, re-wired to git + origin same day;
    the download matched remote HEAD exactly, nothing lost). DIFFERS from A/B: HAS
    Python 3.11 (still no node/gh). Its serve.ps1 derives $root from $PSScriptRoot,
    so it needs NO per-machine path fix (consider copying to A/B). khinsider + GitHub
    raw/api reachable; sounds-resource.com BLOCKED here.

THIS FILE IS NOW TRACKED IN GIT (since v6.1) precisely so it can never be lost when
hopping machines — `git pull` brings the latest handover along with the code.
KEEP IT THAT WAY: update it with every release and PUSH it. It is the single
cross-machine source of truth; the per-machine memory dirs are only quick recall
and do NOT travel.

Per-machine things to CHECK before working (they differ and silently break):
  • .claude/serve.ps1 `$root` must equal THIS machine's repo path (D:\gym app vs
    E:\gym app). It's gitignored, so each machine keeps its own copy — fix it once.
    (Machine B's copy carried machine A's path until 2026-07-11 — fixed; symptom is
    the preview serving HTTP 500 for everything.)
  • The Claude memory directory path differs (see above) — stale facts about "this
    machine" in memory may belong to the OTHER laptop. Trust the handover.
  • Network blocks differ per machine/network: on machine B, Bulbagarden direct AND
    play.pokemonshowdown were BLOCKED (use PokeAPI mirror → pret decomp → Serebii →
    Fandom API → Wayback). On machine A, bulbapedia.bulbagarden.net Special:FilePath
    + a browser UA works fine (v5.19 interiors and v6.1 interiors were fetched that
    way). Probe before assuming.
  • Git identity/credentials are per-machine; deploy verify (GitHub API token via
    `git credential fill`) works on both.
Neither machine has Node/npm or real Python — the preview_eval syntax gate (§9) is
the only syntax check anywhere.

============================================================
3. THE USER (Mateusz)
============================================================
• A gym-goer, not a professional developer. Give click-by-click instructions, explain
  consequences plainly, prefer web-UI over terminal.
• Daily device is an iPhone (Safari, installed as a PWA); dev/build PCs are Windows (two of them, §2).
• Hates on-screen emoji clutter and hates crowded pages — every feature gets its OWN
  page/panel; don't pile content onto existing screens.
• Wants OFFICIAL Pokémon art wherever possible (interiors, artwork, icons).
• Design liberties welcome ("do it as you want") as long as the core need — tracking
  weight per exercise per day — is met.
• Workflow: he requests features conversationally → build → test in the port-8377 preview →
  commit → push → verify the GitHub Pages deploy went green + live → tell him to reopen
  the app on his phone (close fully, open twice, confirm the version string in OPTION).

============================================================
4. CORE TECHNOLOGIES
============================================================
• Vanilla JavaScript (ES5-style, one big IIFE) — NO framework, NO build step, NO deps.
• Single HTML file: index.html (~8300 lines) — all HTML + CSS (one <style>) + JS (one <script>).
• localStorage for all persistence (key `gymtracker.v1`). No backend, no accounts, by design.
• Service worker (sw.js) for offline PWA behavior — precaches the app shell + sprites/maps.
  sprites/art/ (Sugimori artwork) and sounds/ are NOT precached — runtime-cached on first use.
• Web Audio API — GBA-style synth SFX in code AND official audio files:
    - sounds/cries/{key}.mp3   : 122 Pokémon cries (Kanto only; Hoenn needs none, locked)
    - sounds/moves/{slug}.mp3  : 139 authentic FRLG attack-move SFX
• SVG — inline for charts, medals, the egg, ferry/rod/gem/weather icons, athlon cups.
• CSS animations — battle choreography, ball throws, travel ferry, VS splash, evolution
  morph, shiny sparkle, weather overlays, slot reels, the Sootopolis crisis (#crisisfx) +
  Rayquaza descent (#rayfx) overlays, and (v6.1.1) the battle send-out theatre
  (sendOutFx: ball-pop + white materialize for owned mons / pounce-in for wilds, .matz +
  .wildin + .sendball/.sendburst), the full GBA faint sink (faintdrop now drops 190px
  through the arena floor), and the region-aware ferry transition (Hoenn gets wake
  ripples on the S.S. Tidal dot + an Emerald-green arrival sign, day theme only).
Rendering model: a single render() swaps #view.innerHTML by `tab`. State in the `state`
object; every mutation calls save() then re-renders. No virtual DOM — template-string HTML
+ event delegation (one global click listener + one input listener for the BOX search).

============================================================
5. FILE STRUCTURE
============================================================
index.html            — the entire app
sw.js                 — service worker (BUMP `CACHE` on EVERY change!)
manifest.webmanifest  — PWA manifest
handover.md.txt       — THIS file (tracked in git since v6.1 — see §2)
icons/, fonts/        — app icons; Press Start 2P + VT323 (OFL, self-hosted woff2)
map/                  — KANTO: 9 FRLG town maps, center.png, mart.png, kanto.png
  hoenn.png               — HOENN region map (RSE) for the Hoenn TRAVEL page
  hoenn/{...}.png         — 10 official Emerald town maps (littleroot…evergrande)
  gym/{...}.png           — KANTO 13 interiors + HOENN 13 (rustboro…wallace)
  road/{...}.png          — KANTO 9 + HOENN 9 (h-woods…h-victory)
  poi/{...}.png           — KANTO 8 official FRLG interiors (tower, dept, corner,
                            hideout, safari, silph, tower2, lab) + HOENN 12 (v6.1):
                            h-devon, h-corner, h-trick, h-dept, h-wx, h-pyre, h-magma,
                            h-aqua, h-space, h-origin, h-sky, h-seafloor — official
                            Emerald screenshots via Bulbapedia Special:FilePath
                            + (v6.2) h-frontier.png (full frontier map, Serebii
                            pokearth hoenn-em/68) and bf-{tower,dome,palace,arena,
                            factory,pike,pyramid}.png cropped from it locally
sprites/                  — front sprites {dexId}.png; shiny/, back/, back/shiny/ — 306 each
  icons/{dexId}.png       — (v6.4) 306 Gen-3 party menu icons, pret decomp top frames,
                            used by TEAM/BOX lists (fallback: front sprite), precached
  art/{dexId}.png         — 306 official Sugimori artworks, lazy-loaded, NOT precached
  trainers/               — KANTO cast + HOENN cast + (v6.1) archie, maxie, tabitha,
                            shelly, matt, aquagrunt, magmagrunt + (v6.2) the 7 BRAINS:
                            anabel, tucker, spenser, greta, noland, lucy, brandon
                            (all pret/pokeemerald decomp front_pics, corner-pixel
                            MakeTransparent, 64×64)
  items/, arenas/, badges/, npc/ — as before (arenas are shared by both regions)
sounds/cries/, sounds/moves/  — audio (see §4)
.claude/serve.ps1, .claude/launch.json — local preview server (gitignored; $root is
                            PER-MACHINE — see §2!)
.claude/hoenn-gen/     — v6.0 build scratch (PokeAPI CSVs + gen.ps1). GITIGNORED.
docs/poi-blueprints.md — older design notes
docs/screenshots/      — README images (light theme, 390×844 @2x, captured with Playwright)

============================================================
6. DATA MODEL  (state, localStorage key `gymtracker.v1`, schema version 18)
   v6.4 adds: state.bw:[{ts,w}] at the ROOT (bodyweight, shared across regions);
   mons carry cond{cool..tough,sheen}? rib{cat:0..4}? tm[]? (tm now survives import);
   poi gains garden{plots[6],bm} hall{wk,done} shoal{salt,shell,bell,got}
   lotto{d} nav{d}; flags gains berrygift/conteststar (wholesale, no sanitizer work).
   v6.7 adds: game.res {sp:{c,f,v,rw}} (field research, seeded at migration from
   mons+dex in BOTH game and altGame); poi mine{d,u,wall{c{},items[{i,k|m,g}]}}
   and flip{lv,best}; mons carry fx? (entry particle key); every LOG ENTRY now
   stores rel (effort vs the exercise best at log time); flags gains v67note,
   resm1..resm4, shinycharm (all wholesale).
   v6.8 adds: game.perks {used, taken{k:n}, offer:[k,k,k]|null, annLv} (lazily via
   perksOf; TRAINER LEVEL itself derives from logs, never stored); session gains
   gaUsed (GUARDIAN ANGEL, transient); flags gains v68note.
   v6.9 adds: perks gains xp (the hero bar — the ONLY stored level source now)
   and cd (candy-maker set counter); poi.lotto gains n (draws today).
============================================================
{
  version:14, unit, theme:'night'|'day', sound, gamePlayed, dev,
  region:'kanto'|'hoenn',          // which campaign is ACTIVE right now
  altGame: null | {game},          // the PARKED region's save (same shape as game)
  groups, logs, sessions,          // workout data — SHARED across both regions
  session: null | {...},           // live workout + battle (wild carries markers, see below)
  game: null | {
    region, startTs, starter, mons:[{uid,sp,xp,shiny,ts, vits?,held?,tm?[]}], active,
    enc{}, flags{}, seen{}, dex{}, round, boss:{idx,hp}, player:{name,spr},
    money, bp, bestStreak, bag{}, hof[], partyIds, egg, medals{}, hurt{}, rest{}, raid,
    road{}, roadLog{}, daily{}, npcq{}, qSeen, visit, beenTo{},
    poi:{ // per-building state, lazily created via poiOf(k)
      KANTO: tower{floor,clears,weekKey}, corner{coins,date,spins}, hideout{floor,scope},
             silph{floor,card,master,lapras}, tower2{floor,flute,cubone},
             safari{active,balls}, lab{expWeek}, athlon{date,cups}, front{streak,best}
      HOENN (v6.1): devon{f,expWeek}, corner{...same shape}, trick{week,d0..d2,paid0..2,candy},
             wx{f}, pyre{floor}, magma{f}, aqua{f,mb}, space{f}, origin{f}, sky{floor} }
  }
}
v6.1 STORY FLAGS (game.flags, Hoenn save only): arc1..arc8 (chapters), arc8ts (finale
timestamp — drives the Space Program launch count), castformgift, jirachi, v61note.
Wild battle markers on session.wild: tr{...arc:[ch,step]} for story trainers;
pyre:f / sky:f (fight-OR-catch floors); legend:1 (titans/mythics); bg:'arena' overrides
the battle backdrop for dungeon/legend wilds.
Key derived fns: monLevel, statsFor, bossInfo, battleParty, townIdx, curTown, rodTier,
shinyOdds, expShareHolder, poiOf(k), wxFor(town[,dayKey]) (v6.1: optional day for the
forecast board), inHoenn, arcStage() (=first unfinished chapter 1..8, 9=done),
arcHint(), spaceLaunches() (retroactive: weeks since arc8ts with 3+ training days),
feebasTown() (the day's lucky pier), trickOf(). migrateState() runs at INIT.

============================================================
7. GAME SYSTEMS (all live)
============================================================
• TWO CAMPAIGNS: FireRed KANTO + Emerald HOENN (S.S. TIDAL switcher in OPTION after
  the Kanto Champion; each region a full separate save; workout logs shared).
• Pokédex: KANTO = the 151, all catchable. HOENN = the official Emerald regional dex,
  and as of v6.1 ALL 202 ARE OBTAINABLE (fossils via Devon, feebas/milotic via the
  pier, chimecho via Mt. Pyre, stone families via the Dept. Store, titans + Latios +
  Jirachi + Deoxys via the story/post-story, Latias via raid).
• CATCHING: in-battle ball throws, Gen 3/4 capture math, strict per-town AREA_SPAWNS.
• EXP: lifting XP (vol/10 ×mults) + classic wild/trainer EXP + EXP SHARE holder.
• Boss battles: 13 per region on the same hp/lv curve; VS splash; endless rounds.
• THE ROAD / QUESTS / dailies / collectors / raids / eggs / medals — as before, both regions.
• TRAVEL: free-roam ferry between visited towns; DEV MODE opens all towns for preview.
• WEATHER: deterministic per-town-per-day, PURE ART (locked). v6.1 adds the WEATHER
  INSTITUTE's forecast board (today → tomorrow per town) — still display-only.
• TEAM tab (v6.1): shows ONLY the fighting six (battleParty) + an "N more in BILL'S PC"
  row that jumps to the BOX. Full collection lives in the BOX (search/filter/sort).
• ELEVEN KANTO SPECIAL PLACES (unchanged from v5.19): Trainer Tower, Dept. Store,
  Game Corner, Rocket Hideout, Safari Zone, Silph Co., Pokemon Tower, Cinnabar Lab,
  Fishing Pier (3 towns), Pokéathlon Dome, Battle Tower + BP shop.
• TWELVE HOENN SPECIAL PLACES (v6.1) — each with official Emerald art + day palette:
  - DEVON CORP. (Rustboro): story ch.1 + FOSSIL WING (Root→Lileep / Claw→Anorith :L30,
    ₽6,000) + weekly FIELD CONTRACT (same engine as the Cinnabar experiment).
  - GAME CORNER (Mauville): the slots engine reskinned; Hoenn prize shelf
    (Plusle/Minun/Skitty/Trapinch/Bagon/Beldum at :L18) + candy.
  - TRICK HOUSE (Mauville): ORIGINAL v6.1 mechanic — the Trick Master's weekly
    3-puzzle gauntlet: WHO'S THAT POKEMON (dex silhouettes), TYPE MASTER (type-matchup
    quiz), IRON BRAIN (questions about YOUR real training week). ₽400 per pass,
    all three → RARE CANDY. Deterministic per week (strHash(wkKey)); resets Monday.
  - FISHING PIER (Dewford/Mossdeep/Sootopolis): rods from the Mart; Hoenn fish tables;
    FEEBAS only bites at ONE pier town per day (feebasTown(), canon nod to the
    wandering tiles) — the fisherman drops a hint; Milotic by level 30.
  - LILYCOVE DEPT. STORE (Fortree): Hoenn TM corner (14 moves), STONES floor
    (Water/Leaf/Thunder/Moon/SUN — fixes every stone family that had no Hoenn route:
    Ludicolo, Shiftry, Delcatty, Starmie, Vileplume, Bellossom, Wigglytuff, Raichu),
    vitamins floor.
  - WEATHER INSTITUTE (Fortree): story ch.2 (Shelly) + CASTFORM gift + forecast board.
  - MT. PYRE (Fortree): 6 fight-OR-catch ghost floors (Shuppet/Duskull/Banette,
    CHIMECHO on 6F) + story ch.3 summit scene; post-story "linger" encounter
    (chimecho/shuppet/duskull pool) keeps Chimecho farmable.
  - MAGMA HIDEOUT (Lavaridge): story ch.4 (2 grunts + TABITHA + MAXIE);
    post-story TERRA CAVE → battle-catch GROUDON :L70.
  - AQUA HIDEOUT (Mossdeep): story ch.5 (2 grunts + MATT); the second fight's locker
    holds Hoenn's one MASTER BALL (canon).
  - SPACE CENTER (Mossdeep): story ch.6 (grunt + TABITHA + MAXIE, Steven flavor);
    post-story SPACE PROGRAM: 1 launch per calendar week with 3+ training days
    (computed retroactively from logs — no page-open required), 3 launches →
    battle-catch DEOXYS :L30.
  - CAVE OF ORIGIN (Sootopolis, gated until ch.7): SEAFLOOR CAVERN fights
    (2 grunts + ARCHIE) → Kyogre wakes → the CRISIS overlay (Groudon vs Kyogre);
    post-story MARINE CAVE → battle-catch KYOGRE :L70.
  - SKY PILLAR (Ever Grande, gated until ch.8): 5 fight-OR-catch guardian floors
    (Golbat/Claydol/Banette/Altaria/Shelgon) → WAKE RAYQUAZA (the #rayfx descent) →
    finale; post-story: battle-catch RAYQUAZA :L70 at the summit + SOUTHERN ISLAND
    → battle-catch LATIOS :L40 (Eon Ticket).
• THE BATTLE FRONTIER (v6.2, Ever Grande, gate = Hoenn CHAMPION): one POI ('bf') that
  fans into SEVEN facility pages (module global frontierFac routes them; poi-open resets
  it to the hub). All state in poi.bf {tw,dm,pl,ar,fa,pk,py,sym}. Facilities:
  - BATTLE TOWER (bftw): pure streak; every 7th fight = SALON MAIDEN ANABEL.
  - BATTLE DOME (bfdm): 4-round knockout bracket UI; TUCKER is the final of tourney 2
    (silver) and 4 (gold); tourney win +4 BP, rd resets on loss.
  - BATTLE PALACE (bfpl): each fight rolls a SPIRIT mood (×0.85/1/1.25 on YOUR set
    damage — tr.mood consumed in the saveLogEntry dmgT chain); SPENSER every 7th.
  - BATTLE ARENA (bfar): KO within 3 SETS or the judges rule (foe ≤50% HP = decision
    win, else streak ends — wd.jd counter + judgeLoss branch); GRETA every 7th.
  - BATTLE FACTORY (bffa): RENTALS — draft 3 (bf-draft), pick the lead, and a synthetic
    mon (uid 'bfrent', session.bfRent) fights IN PLACE of yours: leadMon() override +
    fm swap in the wild-fight block; rental faint = run over (applyWildCounter special
    case), swap-a-rental-for-the-foe offer after each win; NOLAND every 7th.
  - BATTLE PIKE (bfpk): 10 rooms/run, 3 doors — outcome rolled ON CHOICE (55% fight /
    18% heal / 17% empty / 10% strong, pk.pend gates the fight); LUCY ends run 2 and 4.
  - BATTLE PYRAMID (bfpy): 7 floors, torch-vignette banner lightens per floor, item
    loot each win (BF_PY_LOOT); BRANDON (the Regis) tops climb 2 and 4.
  BRAINS: every brain fight = tr.brain (tier 1 silver / 2 gold / 0 repeat); first two
  wins award the facility's SYMBOL (poi.bf.sym, ceremony modal). 14 symbols total,
  shown on the hub, the facility pages and the TRAINER page (bfTrophyHTML, Hoenn only).
  Blackout in any facility resets that run (triggerBlackout hook). BP: +2 streaks,
  +3 factory, +1 dome/pike/pyramid steps, +4/6/8 clears, +10/20 brains. The hub carries
  a second EXCHANGE CORNER (bpShopRows shared with Kanto's tower). No catching (canon).
  Brain sprites: pret decomp (anabel/tucker/spenser/greta/noland/lucy/brandon); hub +
  7 facility banners cropped from the official Emerald frontier map (Serebii
  pokearth hoenn-em/68.png → map/poi/h-frontier.png + bf-*.png).
• SECRET BASES (v6.3, Hoenn only): one POI ('base') with a DYNAMIC town — POIS entries
  may carry dt:function() and poiPlacesHTML/poiPageHTML prefer it over .town. No base
  yet → the SECRET SPOTS scout page shows in EVERY town (gate: first badge, SECRET
  POWER announcement in HOENN_FEATURES); after setup the base lives in its spot's town.
  Six spots (BASE_SPOTS, towns 0/1/3/4/6/8) with CSS room palettes (BASE_PALS wall+
  floor gradients, .baseroom/.bslotgrid/.bslot). 12 slots hold DOLLS (any CAUGHT
  species — shiny dex entries render shiny sprites, live lookup) or TROPHIES
  (BASE_TROPHIES, 8 SVGs via trophySVG(k): bronze/silver/gold dumbbell by lifetime
  volume 100k/500k/1M, streak banner ≥7, champion cup, frontier crest ≥7 symbols,
  PR plaque 100+ lift, dex diploma — cond() computed live, never stored). MOVE BASE =
  the truck page (baseMoving global), decorations travel. State: game.base
  {spot,deco{0..11:{t:'doll',sp}|{t:'tr',k}}}, sanitized (Hoenn saves only).
• BATTLE THEATRE (v6.3): level-ups of the FIGHTER play IN-BATTLE — queueLevelUp
  diverts into pendingLvCapture (window opened per set in saveLogEntry), the step
  {levelup} flashes the back sprite (.lvflash) + shows the GBA stat sheet (.lvpanel,
  all six deltas) over the arena; other mons keep the stat modal; move-learn modals
  always. Gym badges (idx<8) get playBadgeFx: full-screen badge spin-in → stars →
  flies to the case (bottom-right), THEN the victory modals (the fin() wrapper in the
  boss-victory setTimeout). Low HP: driftBar toggles .dbcrit on the player databox
  (pulses red) + one soft 'lowhp' beep on FALLING into ≤20% (dataset.beeped guard);
  the panel templates pre-set dbcrit when rendering an already-red mon. Tab changes:
  v6.3's stepped GBA wipe felt LAGGY on-device — v6.3.1 REVERTED it to the original
  smooth .22s ease-out fade+rise (do not reintroduce steps() here). Town maps carry a 2-frame
  water shimmer: ONE .h2o div in mapHTML + one CSS block — both commented for easy
  removal if Mateusz dislikes it (he reserved judgment).
• CONTEST SPECTACULAR (v6.4): CONTEST HALL (Petalburg) + BERRY GARDEN (Littleroot) +
  POKENAV (TRAINER page) + LOTTERY (Dept. Store) + ROULETTE (Game Corner) + SHOAL
  CAVE (Mossdeep) — all Hoenn; plus region-free BODY WEIGHT log, PR HALL, PR
  THEATRE, 5 canon CARD STARS and Gen-3 party icons in TEAM/BOX. Full detail in §13.
• THE MAGMA/AQUA ARC (v6.1): 8 chapters in game.flags.arc1..arc8, one per building
  (Devon → Weather Inst → Mt. Pyre → Magma Hideout → Aqua Hideout → Space Center →
  Seafloor Cavern → Sky Pillar). arcStage() gates everything; arcHint() prints the
  next step on every story page; chapter rewards: ₽800+3 Great Balls / ₽2,000 /
  ₽3,000 / ₽5,000 at the finale. JIRACHI: post-story, a 7-day daily streak triggers
  the Millennium Comet gift (:L30) via checkGameEvents.
• Legend fights are ordinary wilds with legend:1 — if they FAINT they pay huge EXP
  and "return to the same spot" (button stays until caught). Master Ball advised.
• v6.7 “FAIR FIGHT” PACK (see §13 for full detail): EFFORT SCALING (damage/XP =
  % of YOUR per-exercise best × the median-best benchmark — one line in
  saveLogEntry feeds everything; ≥95% = guaranteed crit); VOLTORB FLIP at both
  Game Corners (authentic HGSS boards, coins share the slots purse); MINING WALL
  (Pewter museum / Dewford Granite Cave — sets = pickaxe swings); FIELD RESEARCH
  (5 tasks/species, any 4 = researched, ₽400 each, milestones up to the SHINY
  CHARM); ENTRY PARTICLES (8 earned cosmetic send-out bursts, m.fx); MUSCLE
  EMBLEMS (per-group lifetime-set medals on the CARD).
• v6.8 TRAINER CARDS (see §13): Archero-style pick-1-of-3 perk cards, banked by
  the TRAINER LEVEL that grows from real training days. 22 perks, 3 rarities,
  16 engine hooks, the #perkfx card theatre, the PERKS page off TRAINER.
• Nav: 6 docked tabs — TRAINER · MAP · TEAM · LAB · QUEST · OPTION. Routed pages:
  TRAVEL, MART, CENTER, GYMHALL, WORKOUT, TROPHY, CARD, poi-* (all 23 buildings).

============================================================
8. CRITICAL GOTCHAS (read before editing)
============================================================
• BUMP `CACHE` in sw.js on EVERY change (v44 → v45…), or installed phones keep the stale
  version. Then commit AND push — Pages auto-deploys.
• BUMP the app version string in settingsHTML() ("Gym Tracker vX.X") so you can confirm live.
• NO NODE/NPM/PYTHON on either machine. Syntax check = load the preview and probe:
  fetch index.html, extract the <script>, `new Function(src)` in preview_eval —
  parse errors surface there; then execute-probe for runtime errors (see §9).
• ES5 top-to-bottom eval order bites: `var` data tables are assigned at eval time —
  a `X.push(...)` placed ABOVE `var X=[...]` kills the whole IIFE with a blank screen
  and NO console error. Function declarations hoist; data statements cannot.
• REGION TABLES: the content-table globals — TOWNS, TOWN_MAP_XY, WILD_BANDS,
  AREA_SPAWNS, BOSSES, ROAD_LOC, GYMS, RAIDS, RAID_SPECIES, POIS, RIVAL_ROADS,
  TRAINER_CLASSES, NPCQ, DAILY_POOL, DEX_ORDER, FEATURES, and (v6.1) PRIZE_MONS,
  TM_LIST, STONES, STONE_EVOS, FISH_TABLES, FISH_BANDS — are REASSIGNED by
  setRegionTables() based on state.region. KANTO_* captures for the v6.1 shop/fishing
  tables sit NEXT TO their Kanto originals (lines ~5760/6120/6220), NOT next to
  setRegionTables — because `var KANTO_X=X` must run AFTER `var X=[...]` in eval
  order. setRegionTables() only READS them at call time, so its position is safe.
  If you add a per-region table, capture it beside the original and swap it there.
• DEX / MOVESETS / MOVE_TYPES are SHARED SUPERSETS, not swapped. CR_EXT / XP_EXT
  cover ids >151.
• applyImport() sanitizes EACH game object through sanitizeGame(dgame,region).
  Add new game fields inside sanitizeGame — v6.1's poi keys are whitelisted there
  (devon/wx/pyre/magma/aqua/space/origin/sky/trick). game.flags passes through
  WHOLESALE, so story flags need no sanitizer work.
• THE ARC ENGINE: story fights are kind 'arc' with f = chapter*10+step; the wild's
  tr.arc=[ch,step] is validated TWICE (poiStartFight before, poiTrainerWin after)
  against arcStage() AND poiOf(key).f, so stale/replayed fights can't double-advance.
  Chapter completion sets flags.arcN + queues arcScene(N). Chapters 3 and 8 complete
  via page buttons (pyre-summit / sky-wake), not fights. If you add a chapter, keep
  both validations in sync.
• Fight-or-catch floors (pyre/sky) advance in BOTH paths: the wild-faint hook in
  saveLogEntry AND the catch hook in runThrow. Touch both or floors desync.
• State-first, theatre-after: every animated flow (ball throw, slots, travel, athlon,
  fishing, arc scenes, legend catches) decides and SAVES the outcome before the
  animation plays.
• File is BOM-less UTF-8. PowerShell reads need -Encoding UTF8. Use ₽ (not ₱).
  Curly apostrophes (’) inside JS strings are normal here.
• PowerShell here-strings break git commits with embedded quotes — use the Bash tool.
• gh CLI is NOT installed. GitHub API via token from `git credential fill` (Bash):
    TOKEN=$(printf "protocol=https\nhost=github.com\n" | git credential fill | grep '^password=' | cut -d= -f2)
  Deploy verify: poll actions/runs for conclusion=success AND head_sha == your commit,
  then curl the live site for the version string.
• Battle engine is class-based + topmost-scoped: bw()/bq(sel) target the LAST .btwrap
  in the DOM. Don't reintroduce #id lookups. alignBack() sinks the back sprite.
• Building fights are injections into the roadblock engine: poiStartFight → poiWildFor
  builds a wild with a marker; victories resolve in poiTrainerWin (trainers) or inline
  hooks (ghost/pyre/sky/legend wilds). Markers are validated against the building's
  CURRENT floor/streak/stage.
• Night theme skips ALL skins by design (applySkin returns early) — building palettes,
  quest biomes, the Emerald skin AND the v6.1 Hoenn building palettes are DAY-only.
  Do not restyle night. (Verified again in v6.1.)
• Modal queue pops with ~180ms delay — drive modal chains step-by-step when testing.
• Weather is PURE ART (locked). The v6.1 forecast board is display-only too.
• Editing index.html via an external script invalidates the Edit tool's read-state —
  re-Read before the next Edit. Grep for call sites after removing/renaming anything.
• ART SOURCING: probe per machine (§2). Working routes by preference:
    1. PokeAPI mirror on raw.githubusercontent.com/PokeAPI/… (sprites + data CSVs)
    2. pret/pokeemerald decomp (trainers: front_pics 64×64, MakeTransparent corner px;
       tile-based graphics there are palette-stripped — skip)
    3. bulbapedia.bulbagarden.net/wiki/Special:FilePath/<File>.png with browser UA
       (works on machine A; machine B must use Wayback: web.archive.org/web/2023id_/…)
    4. Serebii pokearth (serebii.net/pokearth/maps/hoenn-em/{id}.png)
    5. pokemon.fandom.com API — MUST append &format=png or you get WebP.
       PROVEN on machine C (v6.4.1): action=parse&page=<Article>&prop=images lists a
       page's files, then action=query&titles=File:X&prop=imageinfo&iiprop=url gives
       the static.wikia URL. Gen-III interiors live on CITY articles as
       "<City> - <Building> (Gen III).png" — not on the building's own page.
  Emerald interior/location screenshots are named "<Location> E.png" (or "1F E",
  "entrance E", "SE" for Sapphire/Emerald shares) on Bulbapedia — probe with the
  MediaWiki API (action=query&prop=images) when unsure.
  Resize/crop locally with .NET System.Drawing (PS 5.1). $pid is RESERVED in PowerShell.

============================================================
9. TESTING & DEPLOYMENT WORKFLOW
============================================================
• Preview: .claude/launch.json config "gym-app" runs .claude/serve.ps1 — a PowerShell
  HttpListener static server on port 8377 (both files gitignored, $root per machine §2).
• Resize to mobile (375×812). Plant a full test state into localStorage via preview_eval,
  location.reload(), drive the UI with dispatched clicks
  (el.dispatchEvent(new MouseEvent('click',{bubbles:true}))), read back state/DOM.
• IMPORTANT: changing localStorage does NOT update the RUNNING app (state is already
  in memory) — location.reload() after every planted mutation.
• Syntax gate (no node): in preview_eval —
    sync XHR index.html → match(/<script>([\s\S]*)<\/script>/) → try{new Function(src)}
  catches SyntaxError; if parse-clean but the view is empty, EXECUTE the source in a
  try/catch — the error message + stack line lands you at the bad statement.
• preview_screenshot right after a reload or during a <2s animation often misses it —
  verify via DOM reads instead (this is the reliable path).
• `state` is IIFE-scoped — inspect via JSON.parse(localStorage.getItem('gymtracker.v1'))
  and the DOM.
• Story testing (v6.1): plant flags.arc1..arcN + the matching poi steps
  (wx{f:2}, pyre{floor:6}, magma{f:4}, aqua{f:3}, space{f:3}, origin{f:3}, sky{floor:5})
  and game.visit, then reload. A planted save dumps its whole event-modal backlog at
  once — pop modals in a loop until quiet. Verify region + parked altGame survive a
  switch AND an export/import roundtrip; verify a full wipe boots fresh to Oak.
• CLEAR the test state when done (localStorage.clear() in the preview).
• Commit trailer: Co-Authored-By: Claude <model> <noreply@anthropic.com>.

============================================================
10. MEMORY FILES (persistent per machine — read FIRST in any new session)
============================================================
Machine A (D:\gym app, dctek): C:\Users\dctek\.claude\projects\D--gym-app\memory\
Machine B (E:\gym app, Satsuki): C:\Users\Satsuki\.claude\projects\E--gym-app\memory\
• MEMORY.md — the index. gym-tracker-project.md — versions + deploy rules.
  user-mateusz.md — who Mateusz is. (Machine B also had emerald-campaign-decisions.md
  and asset-pipeline-2026.md — their content is folded into §8/§12 here.)
Memory does NOT travel between machines — THIS handover does (§2). When they disagree,
the handover wins; update the local memory to match.

============================================================
11. LOCKED DESIGN DECISIONS
============================================================
• Muscle group → move type: Pecs=Fighting, Back=Flying, Biceps=Electric, Triceps=Fire,
  Forearms=Rock, Shoulders=Psychic, Legs=Water.
• Real Pokémon with FRLG/Emerald sprites; official GBA look (white boxes, warm frames).
• Official art wherever possible. Big pixel TRAINER SPRITES on the VS screen.
• ANIMATED battle sprites: assessed and DECLINED (2026-07-10). Don't revisit unless
  he asks knowing the tradeoffs (Gen-5 style only, ~25 MB, clashes with FRLG look).
• No harsh punishment — rest days cost nothing. Approved exceptions: boss blackout
  +25%, Battle Tower streak reset, safari flee-on-fail, and Center heals: 10h rest
  free everywhere, OR ₽500 flat (CENTER_FEE) heals the whole team instantly at any
  branch (v6.6.1 — replaced v6.6's free-instant-in-town-0).
• Night theme: REDONE in v6.5 at Mateusz's explicit request (the old “locked” note
  is void). The v6.5 token set (§13) is the new baseline — don't restyle again
  unless he asks. All biome/building skins/palettes remain DAY-only (applySkin
  still returns early at night).
• One points-currency: BATTLE POINTS (Battle Tower + Pokéathlon; BP shop at the Tower).
• Weather is art-only, BOTH regions, forecast board included (locked, he said no
  weather-gated mechanics).
• EMERALD/HOENN: core shipped v6.0; buildings + FULL Magma/Aqua arc shipped v6.1
  (2026-07-11) — fresh start, separate economy, same difficulty curve, official
  Emerald Hoenn dex (all 202 now obtainable).
• THE BATTLE FRONTIER shipped v6.2 (2026-07-11, same day): ALL 7 facilities, Brains
  as bosses, silver+gold Symbols. Scope was locked in advance and delivered as locked.

============================================================
12. BACKLOG / IDEAS DISCUSSED (not built)
============================================================
Ideas (not greenlit): painted Sugimori gym-leader art for the VS screen (manual
asset hunt); time-of-day spawn slices; breeding/natures in the daycare; Bill's PC
favorites; weather MECHANICS (explicitly deferred); dex-completion rewards (diploma);
Johto/Gen-2 content pack (a 3rd campaign — the region model supports it cleanly).

============================================================
13. IMMEDIATE STATE
============================================================
v6.9.2 (2026-09-25) — the first minutes of a NEW game, asked for after trying play.html:
• load() with no save returns seedState() + theme:'day' — new players start LIGHT.
  Existing saves keep their theme; RESET EVERYTHING (seedState) is unchanged.
• pick-starter creates the game with flags {v67note:1, v68note:1}: the EFFORT SCALING
  and TRAINER CARDS what's-new modals only show to saves from before those releases.
  (v68note's SHOW ME jumped to the PERKS page, so new players never saw TRAINER.)
• The starter's LET'S GO now lands on TRAINER (tab 'summary'), not TEAM, and runs
  checkGameEvents() BEFORE render(): it's what creates the road, so rendering first
  showed BROCK as “tap to challenge” on a brand-new game. sw v58.

v6.9.1 (2026-09-25, from C:\Users\Mateusz\Desktop\flights\gym-tracker-pokemon-game) — the
README's TRY-IT page, no game changes:
• play.html: the game in a 390×844 phone frame (scaled to fit the window), for PC
  visitors from the README. Wipes localStorage 'gymtracker.demo' on every visit and
  loads ./?demo, so each visit starts at Oak's intro; RESTART does the same. Screens
  under 600px wide are sent straight to ./?demo full screen.
• index.html: ?demo → KEY='gymtracker.demo' (never the real 'gymtracker.v1') and NO
  service-worker registration (a one-off visitor shouldn't precache every sprite).
• sw.js v57: the navigate handler now only handles the app itself (path ending in
  '/' or '/index.html'). Before, ANY page navigation was cached as ./index.html, so
  opening play.html would have replaced the offline app shell.
• README rewritten with 12 light-theme screenshots (docs/screenshots).

v6.9 “Level Rush” SHIPPED 2026-07-20 from machine C (Desktop) — the TRAINER CARDS
rework Mateusz asked for same-day (“XP bar like my pokemon, more cards, more often,
more rare/epic, loud outlines, new mechanics not just stats”):
• HERO XP: the trainer levels by XP now, NOT training days. perkAddXp(gain) in
  saveLogEntry feeds perks.xp the SAME effort-scaled gain the fighter earns per
  set (verified: 80 kg×10 set mid-session = +120 both). Curve tXpNeed(L)=350+
  90·(L-1) — first levels land mid-workout, settles ~1 level/session, ~70+
  levels/year. trainerLevel()/trainerLvProg() walk the curve from perks.xp.
  The TRAINER page hero card carries a purple “TRAINER LV n” gbar; PERKS page
  bar now shows XP (not days). Migration v20 seeds perks.xp = training-days
  ×1,200 per save (creates perks{} on pre-v6.8 saves too; game AND altGame).
• 15 NEW CARDS (37 total: 13 common / 13 rare / 11 epic), rarity weights now
  5/3/2 (epics show ~½ of hands). New mechanics beyond stats: FAST LEARNER
  (+10% hero XP), BALL SAVER (8% throw refund, runThrow miss branch), ARCADE
  RAT (+10% coin exchange), TIP JAR (₽15/wild KO, appended to the faint line),
  PHARMACIST (+15% battle item heals), SHINY SENSE (+1% shiny odds, additive
  with the charm), BLOCK ARTIST (+25% pokéblocks), PROSPECTOR (+1 wall
  treasure — mineHTML count is dynamic now), SAFARI PASS (+5 balls), CANDY
  MAKER (perks.cd counter → free candy per 25 sets), FOUR-LEAF (10%/stack
  lucky workout, else-if after the LUCKY EGG branch), AMULET HEART (+10% on
  addMoney itself — every ₽ source), OVERDRIVE (PR rel cap 1.25→1.40/1.55),
  DEX SCANNER (uncaught spawn weight ×1.3 in wOf), TWIN TICKETS (lottery
  p.n draws/day; panel + runLotto guard + sanitizer lotto.n). GUARDIAN ANGEL
  is cap 2 now (session.gaUsed is a counter).
• RARITY OUTLINES: .pcard.rar1/.rar2 ::after — pulsing blue ring for RARE,
  double gold/purple glow ring for EPIC (pkpulse), on top of v6.8's shimmer.
• Sanitizer: perks gains xp + cd pins; lotto gains n. Schema v20. sw v56.
• Tested in 8377: parse gate; v6.8-style save (perks w/o xp, 6 days, used 2) →
  v20 seeds xp 7,200 → TRAINER LV 10 exactly on-curve, “7 WAITING”, hero bar
  renders; all-common hand then a DEX SCANNER(epic)+SHARP LENS(rare) hand with
  live computed outlines (gold rgba(232,184,56)/blue rgba(104,160,255)); live
  set +120 hero XP; zero console errors. NOT tested live (code-reviewed): the
  13 new effect hooks' RNG branches (ball refund, four-leaf, twin tickets…).
NOTE: v6.8 shipped hours earlier the same day — its §13 block below stays for
the wiring map (16 original hooks); v6.9 only ADDED hooks, changed the level
source, and widened the pool.

Previous: v6.8 “Trainer’s Path” SHIPPED 2026-07-20 from machine C (Desktop) — the Archero-
style TRAINER CARDS perk system, requested as “3 cards appear, pick one”:
• TRAINER LEVEL: derives LIVE from training days since the campaign started
  (daysSinceGameStart, triangular curve — trainerLevel() = floor((sqrt(8d+1)-1)/2)+1:
  day 1 = LV2, then 3/6/10/15/21/28… days; retroactive, nothing tracked). Every
  level BANKS one pick (perkPicksLeft = level-1 − perks.used). Veteran saves get
  their whole backlog on update — the announce modal (flags.v68note) plays, then
  the chooser auto-opens from the modal queue.
• THE CARDS: 22 perks in PERKS[] — 8 COMMON (cap 5: +6% dmg, −8% taken, +4% dodge,
  +4% crit, +8% EXP, −6% mart, +10% purses, +8% quest ₽), 7 RARE (cap 3 mostly:
  +15% crit dmg, +2 mine swings, +10% catch, −2h rest & −₽100 fee, EXP SHARE 75%
  [cap 1], +1 free spin, +25% hatch), 7 EPIC (caps 1-2: +20% vs bosses/brains,
  GUARDIAN ANGEL once-per-workout 1-HP save [session.gaUsed], ₽250 stipend at
  workout end [joins the recap purse row], +10% team max HP, SECOND WIND 3%-max-HP
  heal step after every set, PERFECT FORM at 90% [pfT()], rare spawn weights ×1.5).
  All BONUS-ONLY per the no-punishment rule. Icons = existing sprites; card art =
  pure CSS (rarity frames bronze/blue/purple, epic shimmer ::before).
• THE THEATRE (#perkfx overlay, .pcard): three cards fly in with a staggered
  rotateY flip + idle float; tap RAISES the card (.sel) and arms the CHOOSE
  button; confirm plays .pwin glow while the others .pgone fly out. STATE-FIRST:
  the 3-card offer is rolled once and SAVED (perks.offer — no reroll by reopening),
  and the pick is applied+saved before the celebration. Auto-chains to the next
  pick while picks remain; “later” closes (offer stays). Rarity weights 6/3/1,
  no duplicate cards in one offer, capped-out perks leave the pool.
• WIRING: perkN(k) reads stacks at 16 engine hooks — wild/boss damage + crit
  chance/multiplier (both formulas), pfT() at all 4 perfect-form sites, dodge/
  reduction/guardian inside applyWildCounter AND applyBossCounter, XP gain +
  eshRate() at all 3 EXP SHARE sites, statsFor hp (player mons only — wilds use
  raw tables), catchCheck+catchOdds (the shown % stays honest), trainer purse,
  quest/daily claim payouts (cqPay/dqPay), mineSwings, egg.left bite, corner
  free spins, centerFee()/restMsNow() (all Center texts now dynamic), spawnWild
  weight fn (wOf), perkWindStep beside both shellBellStep call sites, GOLDEN
  TOUCH + auto-chooser queue in endWorkout, level-up announce in checkGameEvents
  (annLv; toasts instead of opening mid-session).
• UI: PERKS page (tab 'perks', hero button row TRAINER → PERKS with a gold dot
  when picks wait): level bar (days into level), PICK button, the build list
  grouped by rarity with stack chips, how-it-works. Per-region builds (each
  campaign its own path; parked altGame perks are dormant).
• Schema v19 (stamp only). sanitizeGame whitelists game.perks {used, annLv,
  taken cap-clamped vs PERKS, offer validated}. sw v55, ZERO new assets.
• Tested in the 8377 preview: parse gate; fresh boot v19; planted 11-day save →
  LV 5 exactly, “4 picks banked”, announce → auto-chooser from the queue; offer
  state-first in perks.offer; tap→raise→CHOOSE SHARP LENS→win anim→taken/used/
  offer-null→auto-reopen “3 picks banked”; planted 21-stack build renders the
  page grouped by rarity; numeric hooks EXACT (mine chip 4 SWINGS from goldrush 2
  with 0 sets, nurse “4 hours / ₽200” from fieldmedic 3, MAX. HP 248 with +41
  green from vitalspirit 2); live set one-shot an Onix through the ×1.3 stack;
  import roundtrip perks 100%; picksLeft=0 correctly offers nothing; zero console
  errors. NOT battle-tested (code-reviewed): dodge/guardian RNG branches, brain
  titanslayer path, SECOND WIND step visuals.

Previous: v6.7 “Fair Fight” SHIPPED 2026-07-20 from machine C (Desktop) — six features, all
by request (he asked for the effort fix + picked 5 from a research brainstorm):
• EFFORT SCALING (the flagship fix — “side delts feel useless”): battle damage AND
  set-XP no longer scale with raw kg. In saveLogEntry, vol = rel × powerBase() ×
  r × s, where rel = entry.w / prevBest (capped 1.25, =1 on a first-ever log) and
  powerBase() = MEDIAN of all per-exercise bests (min 20, lives next to bestW).
  entry.rel is stored on every logged set (survives delete/undo untouched).
  rel ≥ 0.95 → GUARANTEED CRIT in both wild and boss paths (“PERFECT FORM —
  critical hit!”); beating a PR overdrives damage up to 125% and still triggers
  the old candy-level + PR theatre. One-time NEW RULES modal via flags.v67note
  (both regions). Lifetime-volume trophies/ranks still use RAW tonnage.
• VOLTORB FLIP (Game Corner, BOTH regions): the full HGSS table — VF_LEVELS holds
  the authentic 8-level × 5-variant [×2,×3,volt] boards; 5×5 grid, per-line SUM +
  VOLTORB hint plates (VF_COLS row colors), FLIP/MARK modes, pot = product of
  flipped multipliers. Clear all ×2/×3 → pot banks into poi.corner.coins (the
  SAME purse the slots feed — a free skill road to the prize shelf), level +1
  (max 8), poi.flip {lv,best}. Voltorb → level drops to #cards flipped (min 1).
  Board lives in module global vfB (transient; leaving forfeits, vfB.rg guards
  region switches). Corner seg is now region-aware: Kanto SLOTS|FLIP, Hoenn
  SLOTS|ROULETTE|FLIP; poi-open resets cornerGame to slots.
• MINING WALL (Kanto: MUSEUM DIG SITE, Pewter town 1 — its first POI; Hoenn:
  GRANITE DIG SITE, Dewford town 2): every SET logged today = 1 pickaxe swing
  (retroactive via daySetsMap, swings = setsToday − poi.mine.u, u resets with the
  d day-stamp). 8×5 wall, 4 treasures rolled STATE-FIRST at wall creation
  (mineNewWall: weighted loot — cash 400/1200/5000, balls, meds, vitamins, rare
  candy, berries in Hoenn via BERRY_T1); cleared dirt GLINTS when an unfound
  treasure is in the 8-neighborhood (mineNear). All 4 found → fresh wall
  instantly. Wall persists across days; only swings are daily. FEATURES announce
  in both regions (feat_mine, town 1 / town 2).
• FIELD RESEARCH (PLA-style, BOTH regions): 5 tasks per species — obtain 1 /
  obtain 3 / defeat 5 / win 3 with it / (evo species: own its evolution, finals:
  raise to :L30, live-computed) — ANY 4 = RESEARCHED, so unique legends can
  finish. Counters in game.res[sp] {c,f,v,rw}: c bumps in registerDex (catches,
  gifts, hatches, evolutions), f + v in the wild-faint hook, v also on the boss
  finisher. Per-species claim ₽400 on the dex-art sheet; dex grid shows a gold
  pulsing dot (claimable) or green tick (researched). Milestone board above the
  grid: 10→₽2,000, 30→3 candies, 60→₽10,000, 75% of the regional dex (114
  Kanto / 152 Hoenn) → THE SHINY CHARM (flags.shinycharm — shinyOdds() was
  ALREADY wired to double 5%→10%, the charm just finally exists). Milestone
  flags resm1..resm4 ride wholesale. Migration v18 seeds res from owned mons +
  dex for game AND altGame.
• ENTRY PARTICLES (cosmetic, BOTH regions): 8 CSS-only battle-entry flourishes
  (PARTICLES table: sparkle free, confetti 1 badge, bubbles 30 species, flames
  7-day streak, lightning 1,000 lifetime sets, leaves 250k volume, hearts 20
  mons, gold burst champion) — unlocks are LIVE conds (retroactive, nothing
  stored, base-trophy style). Equipped key on the mon (m.fx, sanitized); picker
  panel on the mon sheet under HELD ITEM; spawnEntryParticles fires inside
  sendOutFx for player ball sends (+650ms), .ptcwrap div in .bt-pscene,
  auto-removed. leadMon() resolves the enterer (bfrent rental = no fx, fine).
• MUSCLE EMBLEMS (Trainer CARD, region-free): lifetime SETS per muscle group —
  the fair yardstick, matching the release theme — bronze 300 / silver 1,000 /
  gold 3,000 (EMBLEM_TIERS). Seven hex-shield SVGs (emblemSVG: metal fill by
  tier, rim = the muscle's move-type color) in a CARD panel; tap → progress
  modal. Live-computed from logs via groupKeyOf (emblemSets) — nothing stored.
• Schema v18 (res seed + stamp). sanitizeGame additions: poi.mine (validated
  wall), poi.flip, game.res, mon.fx. sw v54 — ZERO new network assets (arenas
  cave art, item sprites, sprite 100 = Voltorb, everything already precached).
• Tested end-to-end in the 8377 preview: parse gate; fresh boot v18; planted
  Kanto save — effort math EXACT (12kg@12 best → rel 1, 108 XP with the 1.5×
  session mult vs 21 pre-v6.7; 6kg → rel .5, 54 XP; 13kg PR → rel 1.08, 117 XP,
  PR theatre, perfect-form kill); res tallies bumped in live fights (zubat f:1,
  charizard v:4); particles panel gates right (3 unlocked on the planted save),
  flames wrap spawned on a real switch send-out (12 spans, none for the bare
  mon); emblems 2/21 (1,206 leg sets = silver, modal math right); mining 9
  swings from real sets, nugget +₽400, glint on the true neighbor; Voltorb Flip
  L1 board CLEARED for exactly 24 coins (the [3,1,6] max) → banked, lv 2, then
  bust → lv 1 with bank untouched, MARK dot works; dex board 13/151 with 13 gold
  dots, muk sheet 4/5 → ₽400 claim → tick, milestone ₽2,000 claim; Hoenn: mine
  at Dewford, corner seg 3 tables, own coin purse, own v67note/feat_mine;
  import roundtrip 100% survival of res/rw/fx/mine/flip/coins/resm1 both saves;
  zero console errors throughout. NOTE: planted-state writes can be overwritten
  by stray app save() timers — write AFTER a fresh reload settles (bit me twice).
  Also: town visit clamps to badge-unlocked towns at boot (planted Hoenn visit 3
  reverted until boss.idx=4) — not a bug, just a planting gotcha.

Previous: v6.6.1 SHIPPED 2026-07-13 from machine C (Desktop), two tweaks on v6.6 by request:
• INSTANT HEAL IS NOW PAID + EVERYWHERE: the v6.6 town-0-free special is GONE.
  Every Center branch offers “🔔 INSTANT HEAL — ₽500” (var CENTER_FEE=500, one
  knob) under the sick bay: whole team on the spot, disabled with “(not enough ₽)”
  when broke, money guard + toast in the center-heal-all action (fee charged only
  if someone actually needs healing). The FREE 10h rest stays in every village,
  and the heal CEREMONY modal (tray + chime) is unchanged. Fee sanity: less than
  one Full Restore (₽2,500 heals ONE mon) but real money early game.
• GYM DOORS GATE AT THE MAP CLICK: tapping BOSS on the town map with !roadClear()
  now pops gymGuideModal() (the Gym Guide, same words as the locked hall page,
  with a TO THE QUESTS button) and NEVER opens the arena. The v6.6 locked
  gymhall page remains as the second lock for direct tab navigation.
• Tested in the 8377 preview: blocked map click → guide modal, stays on map;
  ₽9,000→₽8,500 after a 2-mon heal (hurt+rest cleared, ALL HEALED tray);
  broke trainer (₽120) → disabled button; cleared road → map click opens the
  gym straight to TALK TO MISTY; zero console errors. sw v53.

Previous: v6.6 “League Rules” SHIPPED 2026-07-13 from machine C (Desktop), all by request:
• GYM PAGES CLEANED: the pinned bouncing leader sprite + name tag are GONE from the
  gym interior (the official screenshots already show the leader standing there —
  gy.lx/gy.ly in GYMS are now unused but kept). The leader's TEAM renders as a
  sprite row in the info panel (beaten members fade), and the challenge moved to a
  proper “TALK TO <LEADER>” button. If Mateusz reports any gym whose official
  interior LACKS the leader, fetch the leader version from machine A / Fandom
  (city article, “<City> - Gym (Gen III).png”).
• GYM DOORS LOCK (like the real games): !roadClear() now blocks the ENTIRE gym page
  — dimmed interior + 🔒 tag + a GYM GUIDE hint + TO THE QUESTS button. stadium-go
  keeps its own roadClear guard as the second lock.
• NIGHT WEATHER OFF: wxChipHTML/wxOverlayHTML return '' unless theme-day (sun over
  a sleeping town broke the night illusion; no official night maps exist for Gen 3).
  Weather STAYS pure art in day (locked rule unchanged); the Institute board is
  untouched (indoor info).
• HOME-TOWN INSTANT HEAL: the town-0 POKEMON CENTER (Viridian/Littleroot) heals the
  whole team FREE and INSTANTLY — “🔔 HEAL MY TEAM” under the sick bay →
  center-heal-all action (deletes hurt+rest for all mons) → the heal CEREMONY modal
  (nurse, .healslot tray of party icons blinking back one by one, new 'heal' sfx =
  the rising center chime). Other towns keep the 10h rest system + a hint that the
  home branch heals instantly. §11's “10h Center heals” exception now reads:
  EXCEPT town 0 (Mateusz's request — home comfort, like the games).
• Tested in the 8377 preview: locked page (dim/guide/no talk btn/quests btn), open
  page (no overlay, team row 74+95, TALK TO BROCK), town-0 heal (2 needy → tray 2,
  hurt+rest cleared, ALL HEALED ceremony), town-1 regression (REST 10H intact, no
  heal-all, hint present), night map = 0 chips/overlays, day path untouched
  (two-line early return), zero console errors. sw v52.

Previous: v6.5 “Night Life” SHIPPED 2026-07-13 from machine C (Desktop), all by request:
• PLACES = STYLE 1 “BIG ART” EVERYWHERE (Mateusz's pick): full-width interior art
  (92px cover) with the name on a bottom gradient strip, status chip top-right, sub
  text under the card; locked doors render as dark silhouette cards with their
  gateTxt hint. The Celadon 3-style demo is DELETED (poiPlacesDemoHTML is gone).
• THE THEATRE FAMILY grew: playTownFx (badge win → the newly-opened town's map art
  framed in gold, then a SET SAIL town card — the old badge-modal town sentence
  moved there), playHatchFx (egg wobbles → white burst → newborn pops + reveal
  modal replaces the old two-modal chain), playFinishFx (end-workout: TRAINING
  DONE! + duration over the ace's art, then a REDESIGNED recap modal: vbanner +
  2×2 stat tile grid + purse row). ALL fx-then-modal flows use the fxHold gate.
• MODALS MODERNIZED globally: springy rise-in (.34s cubic-bezier(.2,1.25,.4,1)),
  deep drop shadow, blurred backdrop (backdrop-filter 3px).
• NIGHT THEME REDONE (Mateusz lifted the old “night is locked” rule himself —
  §11 note superseded): proper dark-theme tokens per Material guidance — bg
  #0E1116 (near-black blue, matches the meta theme-color now), elevation steps
  #141A21 → #1A212B → #232B37, borders #3A4656 slate (the day theme's orange
  no longer burns on dark), text #E7ECF2/#93A0B4 (~88%/60%), desaturated night
  accents (--red #E5726A coral, --accent #4FAE72, --gold #E2B85C, --hp/--exp
  softened), warm-tinted acc buttons (#2E2018 + #F0B896 text).
• NIGHT TOWNS: fixed a LONG-broken rule — `filter:var(--townf,none) brightness(…)`
  resolved INVALID at computed-value time (--townf never existed), which reset
  filter to none, so town maps were never dimmed at night. Now a real moonlight
  pass: brightness(.55) saturate(.68) contrast(1.05) + a radial moon-glow /
  deep-blue gradient veil (mapwrap::after). Lesson: never chain var() with a
  'none' fallback into a filter list.
• BACK-SPRITE HARDENING (the recurring “cut in half”): alignBack's canvas pad
  scan can return an ALL-TRANSPARENT read on iOS under memory pressure — pad
  then measured the whole sprite and placeBack sank the mon 40-78px below the
  floor, CACHED for the fight. Now: an empty scan counts as pad 0, and pad
  clamps to 18 rows max. (Plus v6.4.1's settleEntrance guard for the stall case.)
• sw v51. Version string v6.5 “Night Life”.

Previous: v6.4.1 SHIPPED 2026-07-13 from machine C (Desktop), all by request:
• OFFICIAL INTERIORS: Contest Hall (map/poi/h-hall.png, 240×160 Gen-III lobby) +
  Shoal Cave (h-shoal.png banner, h-shoalice.png inside THE ICE ROOM panel) — found
  via the pokemon.fandom.com API on machine C (action=parse&prop=images on the
  Lilycove_City / Shoal_Cave articles → imageinfo URL → &format=png). Bulbapedia is
  Cloudflared on machine C; Fandom is NOT — add that to the art-route list. The
  GARDEN keeps its CSS banner deliberately (original mechanic, no canon interior;
  Fandom's Berry_Master.png is anime art — rejected).
• ROAD/TRAINER-FIGHT SPRITE FIX (the "cut in half" bug): trainer/boss starts hide the
  player's back sprite behind a 1730ms animation-delay with backwards fill (matz).
  If iOS drops/stalls that delayed animation (VS-splash compositing, Low Power Mode),
  NOTHING stripped the class until the first set — the mon stranded half-materialized.
  sendOutFx now arms settleEntrance(spr,ms) — a token-guarded timer that force-removes
  matz/wildin and clears the inline delay right after the entrance window (dly+950 for
  balls, dly+700 for wilds). A newer entrance invalidates older timers (sendTok).
• VS SPLASH FLIPPED to the classic FRLG arrangement: the opponent's blue banner rides
  TOP (slides in first), your red banner rides BOTTOM (answers .12s later). One CSS
  swap on #vsfx .vsl/.vsr — playVsFx untouched, covers roadblocks/bosses/POIs/brains.
• UNLOCK THEATRE: every FEATURES announcement now bursts a PR-style full-screen
  overlay first (playUnlockFx: #unlockfx, gold-blue, icon at 96px, 2.3s) and shows
  the detail modal 1.9s in. NEW: fxHold — queueModal treats the fx gap as occupied,
  or parallel announcements clobber each other (the modal queue only knew about
  #modalwrap.open). Any future "fx then modal" flow MUST set/clear fxHold the same way.
• PLACES DESIGN VOTE: poiPlacesDemoHTML renders THREE candidate looks stacked, in
  KANTO CELADON ONLY (poiPlacesHTML branches on !inHoenn()&&t===4): STYLE 1 BIG ART
  (92px cover image, name on a bottom gradient, chip top-right, sub underneath),
  STYLE 2 POSTER GRID (2-col tiles, 70px image, centered name+chip, no sub),
  STYLE 3 TICKET LIST (78×58 thumb, 6px palette-colored left spine, classic text).
  All buttons live. WAITING ON MATEUSZ'S PICK — then apply the winner to every town
  and DELETE the demo + this note.
• Tested in the 8377 preview: parse gate; live road fight (splash: enemy top y14,
  player bottom y374; sprite settles class-clean/76px/grounded at 2.6s); unlock fx
  wiring proven via the delayed modal + CSS validated full-viewport; Celadon shows
  all 3 styles (9 live buttons, style-2 tile tap navigates); town 3 keeps the classic
  list; hall/shoal banners render the official art (LOW TIDE tag works); zero console
  errors. NOTE: the Browser pane screenshot tool was wedged AGAIN this session —
  geometry was verified via getBoundingClientRect, not pixels.

Previous: v6.4 “Contest Spectacular” SHIPPED 2026-07-13 from machine C (Desktop) — the biggest
single release yet. Everything below is LIVE and preview-tested end-to-end:
• CONTEST HALL (Petalburg, POI 'hall'): weekly contests in 5 categories mapped to REAL
  muscle groups (catOfGroupName: tough=Legs+Fore arm, cool=Pecs+Triceps, beauty=Back,
  cute=Biceps, smart=Shoulders+abs — muscle words match BEFORE motion words, and it's
  "fore\s?arm" because the default group is spelled "Fore arm"). Condition =
  pokéblock feed (mon.cond, permanent, sheen-capped 255) + this week's real sets ×4
  (capped 120). One entry per category per week (poi.hall {wk,done}, lazy Monday
  reset). Rank ladder NORMAL→SUPER→HYPER→MASTER per mon per category (mon.rib 0..4);
  contest = visual round (condTotal vs 3 deterministic rivals, strHash(wk+cat+rank))
  + 5 appeal rounds (move contest-types via CONTEST_MOVE_CAT, combo bonuses, light
  RNG rolled state-first) → 3-modal theatre. Ribbons render as inline SVG rosettes
  (ribbonSVG) on the mon sheet + PokéNav. All 5 MASTER on one mon → flags.conteststar.
• BERRY GARDEN (Littleroot, POI 'garden'): 6 plots; growth = TRAINING DAYS since
  planting (retroactive from daySetsMap, no page-open needed; ripe at 3). Harvest
  2 (+1 on a 4-training-day week). 10 berries as bag ITEMS (5×tier-1 ₽200 at the
  seed stand, 5×tier-2 from the weekly BERRY MASTER gift). Welcome basket once via
  flags.berrygift. POKEBLOCK BLENDER lives on the hall page: berry+mon → permanent
  cond gain ((18|30)+2×weekTrainDays), sheen +14/+22, blocks are instant (no block
  inventory).
• POKENAV (routed tab 'nav', Hoenn only, button on TRAINER page): MATCH CALL — one
  deterministic daily rematch (navCaller: hash of day+startTs; kind 'call' injection;
  win stamps poi.nav.d via poiTrainerWin tr.call branch); TRAINER'S EYES — gym leader
  gallery with beaten state; CONDITION — party contest bars + ribbons.
• LOTTERY (Dept. Store panel, Hoenn only): daily 5-digit draw (strHash of day+startTs)
  vs each mon's hidden ticket (strHash of uid); trailing matches: 2=vitamin,
  3=rare candy, 4=₽10k, 5=MASTER BALL. poi.lotto{d}. State-first, digit-reveal modal.
• ROULETTE (Mauville Game Corner, Hoenn): SLOTS|ROULETTE seg (module global
  cornerGame); 12 cells (4 species rows × 3 color cols), 5c a bet, pays 12×/4×/3×;
  result rolled BEFORE the ball sweep (.roulon cell chase). Shares corner coins.
• SHOAL CAVE (Mossdeep, POI 'shoal'): tide = parity of local epoch-day (tideLow);
  collect 1 salt (low) or shell (high) per day (poi.shoal {salt,shell,got});
  4+4 → forge SHELL BELL (held item: heals the fighter min(12% max HP, dmg/400)
  after each set — shellBellStep beside leftoversStep in BOTH the wild and boss
  paths). ICE ROOM: plain battle-catch wilds (Spheal 60/Snorunt 25/Zubat 10/Golbat 5).
• TRACKER HALF: BODY WEIGHT page (tab 'body', from OPTION + CARD): state.bw at the
  ROOT (shared across regions, sanitized in applyImport, capped 2000), SVG trend
  chart, add/delete entries. PR HALL (tab 'prhall', from CARD): every exercise's
  best as ranked plaques. PR THEATRE: every new record plays a full-screen
  celebration (#prfx overlay, Sugimori art of the active mon, auto-dismiss 2.6s) —
  fires from saveLogEntry OUTSIDE the game branch, so it works with no game too.
• TRAINER CARD: 5 canon CARD STARS (champion / full dex / 7 frontier symbols /
  contest star / 1M lifetime volume) as a gold-star row (canonStars), plus PR HALL
  + BODY WEIGHT link buttons.
• GEN-3 PARTY ICONS: sprites/icons/{dexId}.png — all 306, cropped top frames of
  pret/pokeemerald graphics/pokemon/*/icon.png (they ARE correctly paletted; the
  handover's old "palette-stripped" warning applies to tilesets, not icons).
  TEAM rows + BOX grid render icons (monIconPath/monIconImg, onerror→battle sprite).
  Precached in sw (~350B each).
• PERF PASS: BOX/DEX grids lazy-load images (loading=lazy decoding=async), BOX
  search debounced 120ms, lists moved from 2-3KB sprites to ~350B icons.
• BUGFIXES: sanitizeGame now keeps mon.tm on import (taught TMs used to VANISH on
  every restore — pre-existing since v6.1); catOfGroupName order fixed so "Leg
  press"-style names stay TOUGH; hallPickCat revalidated against findMon (stale
  uid after import/region switch).
• Schema v17 (root bw init + stamp). sanitizeGame whitelists poi
  garden/hall/shoal/lotto/nav + mon cond/rib/tm; game.flags rides wholesale
  (berrygift/conteststar free). sw v49 precaches 306 icons + 13 item sprites
  (10 berries, shellbell, shoalsalt, shoalshell — PokeAPI).
• ART NOTE for machine A: hall/garden/shoal use arena-art banners + CSS chrome;
  official Emerald interiors (Bulbapedia "Lilycove Contest Hall E.png" etc.) are a
  5-minute polish fetch where Bulbapedia works (blocked on machines B+C).
• Tested in the 8377 preview end-to-end (fresh boot schema v17; planted champion
  save; garden plant/retro-growth/harvest/master; blender cond+sheen; full contest
  → ribbon + week lock; shoal collect/forge/ice-chooser; lottery draw+lock;
  roulette bet/sweep; PokéNav 3 views + LIVE Match Call fight through a real
  workout set (victory, purse, nav stamp) + PR theatre overlay fired same set;
  body/prhall/card; region switch parks everything; Kanto regression ZERO leakage;
  import roundtrip 100% survival; day-theme POI palettes; zero console errors).

Previous: v6.3.1 SHIPPED 2026-07-12 from machine C (Desktop). Two changes, both by request:
• TAB TRANSITION REVERT: the v6.3 steps(4) GBA page wipe felt laggy on his phone —
  restored the exact v6.2 .pagein (fade + 8px rise, .22s ease-out; recovered
  byte-for-byte from git a4ad8d1). The trigger JS was untouched in both versions.
• OFFICIAL CLICK SOUND: sfx('click') now plays the REAL Gen-3 SE_SELECT instead of
  the old approximate 920Hz blip. No audio file — the GBA itself synthesizes this
  sound from sequence data, so seSelect() renders that data exactly via Web Audio:
  50%-duty square, A#6 25ms → G7 125ms, velocity trembling 68/127/68/127/68, +0.4st
  bend, instant attack/fast release (source: pret/pokeemerald se_select.mid +
  voicegroup rs_sfx_1 voices 87/88, decoded with machine C's Python). Fires on every
  [data-action] tap via the one global listener (first tap still plays 'title').
  Zero new network assets; loudness knob = V in seSelect() (0.05).
sw v48. Verified in the 8377 preview: parse gate OK, live CSSOM shows ease-out rule,
tab taps through the real handler created exactly the expected oscillator graph
(4-tone title + 1-osc select), nav/renders intact, zero console errors.

Previous: v6.3 “Secret Base” SHIPPED 2026-07-11 from machine B (E:\gym app) — the 4th release
of the day. Secret Bases (6 spots, doll+trophy decorating, moving truck) + the
battle-theatre pack: in-battle level-up flash & stat sheet, gym badge ceremony,
low-HP heartbeat databox + beep, GBA stepped page wipe, and the 2-frame water
shimmer on town maps (isolated to one div + one CSS block — Mateusz may ask to
remove it). Schema v16 (stamp only; game.base whitelisted in sanitizeGame, Hoenn
only), sw v47, ZERO new network assets (dolls = existing sprites, trophies = inline
SVG). Tested in the 8377 preview via DOM (the Browser pane's screenshot capture was
wedged this session; JS eval worked fine): base scout/setup/decorate/move + dynamic
town row, level-up in both regions, badge fx (h-dynamo + modal chain), dbcrit live
toggle, import roundtrip (base survives), Kanto regression (no base row), fresh
wipe, zero console errors. NOTE for testers: travel is blocked mid-session
(“Finish your workout”) and gym challenges need roadClear() — plant road
{mb:{0:1,1:1,2:1},qdone:{0..7:1}} to force it.

Previous: v6.2 “Battle Frontier” (same day, machine B). The short list:
• All 7 facilities live behind ONE Ever Grande POI (champion-gated), each mechanically
  distinct (streaks / bracket / mood / judge clock / rentals / doors / dark climb).
• 7 Brains from the decomp, 14 SYMBOLS (enamel SVGs, silver→gold), ceremony modals,
  symbols on hub + TRAINER page, second BP shop at the hub (bpShopRows shared).
• Schema v15 (stamp only — poi.bf is whitelisted in sanitizeGame: streaks/best/dm/fa
  incl rental team validation/pk/py/sym). sw v46 precaches h-frontier + 7 bf banners
  + 7 brain sprites.
• BUGFIX folded in: townIdx() capped Hoenn at town 8 (Sootopolis) FOREVER — badgeCount
  maxes at 8, Ever Grande is town 9, so SKY PILLAR (ch.8!) and the frontier were
  unreachable for legit players (dev-mode masked it in v6.1 testing). Now 8 badges
  opens every town; Kanto behavior provably identical (league town IS index 8).
• Tested end-to-end in the 8377 preview: every facility loop + its brain (Anabel/
  Tucker/Lucy/Brandon fought and won, symbols awarded; palace mood + arena judge both
  paths; factory rental verified ON the field, switch blocked, swap works), stale
  frontierFac hub-reset, Kanto regression (tower/shop intact, no symbols/frontier),
  import roundtrip of full bf state, region switch, night theme, fresh wipe, zero
  console errors. NOT UI-tested (code-reviewed only): blackout facility resets, gold-
  tier rematches (same path as silver, tier=2).

Previous: v6.1.1 “Battle Theatre” (same day, machine B) — pure presentation:
• FERRY FIX: playTravelFx was hardcoded to map/kanto.png — Hoenn travel drew its route
  over the KANTO map. Now regionMapImg()/regionName(), plus #travelfx.hoenn flavor:
  wake ripples behind the S.S. Tidal dot (::before/::after, both themes) and an
  Emerald-green arrival sign (html.theme-day scoped — night untouched).
• SEND-OUT THEATRE (both regions): sendOutFx(side,kind,delay) — trainer/boss/player
  mons get a Pokéball pop + white-flash materialize (.sendball/.sendburst/.matz,
  backwards fill hides the sprite through the delay); wild mons pounce in (.wildin).
  Wired into: battle start (pendingEntrance global, consumed by a render() hook —
  trainer/boss starts pass dly:1450 so entrances clear the VS splash; the boss cameo
  keeps its own enemy choreography), runBattleStep st.slide (post-KO sends, next boss
  member decides ball-vs-wild off session.wild.tr), poiStartFight live-session swaps,
  post-catch/flee next wild in runThrow, and do-switch. sprFx now also strips
  matz/wildin and clears inline animationDelay (it's reused for stagger — MUST be
  cleared or later lunge/blink/faint start late).
• FAINT: faintdrop now sinks 190px through the arena floor (arenabg overflow clips
  it) instead of the old 36px dip. Same class/step engine, no call-site changes.
Tested end-to-end in the 8377 preview: full choreography timeline via MutationObserver
(lunge→blink→faintdrop→wildin), switch, catch + break-free ball theatre regression,
boss VS-splash/cameo interplay, Kanto ferry regression, Hoenn ferry + night probe,
Hoenn wild spawn, fresh-boot wipe, zero console errors.

Previous release: v6.1 “Magma & Aqua” SHIPPED 2026-07-11 from machine A (D:\gym app).
THE FULL HOENN CAMPAIGN IS LIVE: 12 special places (§7), the 8-chapter Magma/Aqua
arc (flags.arc1..arc8 + arcStage()/arcHint()), all 202 Hoenn dex obtainable
(fossils/feebas/milotic/chimecho/stone families/titans/Latios/Jirachi/Deoxys),
post-story legend battle-catches (Rayquaza/Kyogre/Groudon :L70, Latios :L40,
Deoxys :L30 via the Space Program, Jirachi via 7-day streak), TEAM tab shows only
the fighting six (+BOX row), Trick House weekly quiz gauntlet, Weather Institute
forecast board (art-only), Hoenn Dept./Corner/Pier region tables, crisis + Rayquaza
CSS overlays. Schema v14 (no data migration needed — flags/poi are freeform),
sanitizeGame whitelists the 9 new poi keys, sw v44 precaches the 12 interiors +
7 Magma/Aqua trainer sprites.
Tested end-to-end in the 8377 preview (chapter fights, wake sequence, legend
catch via Master Ball, launch math, trick quizzes, stone evolutions, feebas hint,
import roundtrip, region switch both ways, night theme untouched, Kanto regression).
KANTO IS UNTOUCHED except: TEAM tab party-only view (both regions) and the shared
poiPlacesHTML/poiPageHTML gate-text parameterization.

One-line orientation: it's ONE vanilla-JS file with localStorage state — read §2
(two laptops!), honor §8's gotchas (sw bump + version bump every change, no node,
data-statement ordering, region-table captures), test on port 8377, and never ship
without seeing the deploy green and the new version string live.
